# Overnight run — 2026-04-21

Single autonomous push by Cursor while Matas was asleep. Working
through the 9-task list in `docs/cursor-prompts/OVERNIGHT-RUN-2026-04-21.md`.

## Pre-flight

- `gh pr view 8 --json state -q .state` → **OPEN** at start of run; polled again
  after ~7 min, still **OPEN**. Tasks A, F, G all depend on the
  `campaign_drafts.event_id` column being live on `main`. Polling
  for the full 20-min window would burn night time for nothing
  (Matas is asleep), so the schedule is rebalanced:
  1. Run the six tasks that are independent of #8 first (B, C, D,
     E, H, I).
  2. Re-check #8 after they land.
  3. If #8 is merged by then, do A, then F, then G in order
     (F and G both depend on A).
  4. If #8 is still open, log the three as "deferred — pending #8".
- `git checkout main && git pull` → already up to date with
  `origin/main` at `019e766`.
- `npm run build` on main → **PASS**.
- `npm run lint` on main → baseline locked.

### Lint baseline

```
✖ 61 problems (13 errors, 48 warnings)
```

The bar for every overnight task is "introduces zero new errors or
warnings vs this baseline in files the task touched". Pre-existing
issues in untouched files are out of scope.

## Task results

| Task | PR | Status | Summary |
|------|----|--------|---------|
| B | [#9](https://github.com/matas-offpixel/meta-campaign-builder/pull/9) | merged | `as ResolvedShare` cast replaced with discriminated union; resolver returns `malformed` for inconsistent rows; call sites narrow on `share.scope`. |
| C | [#10](https://github.com/matas-offpixel/meta-campaign-builder/pull/10) | merged | Ticketing provider abstraction + Eventbrite adapter; new migration 029 (3 tables, RLS, triggers); 5 API routes; client settings page + connections panel; 4TheFans stub registered for Task D. |
| D | [#11](https://github.com/matas-offpixel/meta-campaign-builder/pull/11) | merged | 4TheFans adapter behind `FEATURE_FOURTHEFANS_API`; fetch wrapper + provider with TODO blocks pointing at `docs/ticketing/fourthefans-onboarding.md`; `.env.local.example` extended; `FourthefansDisabledError` typed subclass for clean error handling. |
| E | [#12](https://github.com/matas-offpixel/meta-campaign-builder/pull/12) | merged | Vercel-Cron `/api/cron/sync-ticketing` (bearer-auth via `CRON_SECRET`, service-role client, per-link error isolation); `/api/ticketing/connections/[id]/health` for the dashboard health pill; `vercel.json` schedules nightly at `0 3 * * *` (Hobby cap is 1×/day — original 6h cadence rejected at deploy, downgraded). |
| H | [#13](https://github.com/matas-offpixel/meta-campaign-builder/pull/13) | merged | D2C comms scaffolding — migration 030 (3 tables, RLS, triggers); 4 stub providers (mailchimp/klaviyo/bird/firetext) all behind `FEATURE_D2C_LIVE` with shared dry-run helper; 6 API routes (connections, templates, scheduled); D2C settings panel on client + per-event comms planner. Defence-in-depth: provider, route, and schema all enforce dry-run while flag is off. |
| I | [#14](https://github.com/matas-offpixel/meta-campaign-builder/pull/14) | merged | Creative templates scaffolding — migration 031 (`creative_templates` + `creative_renders` w/ RLS); `CreativeProvider` interface; Canva/Bannerbear/Placid stubs each behind their own `FEATURE_*` flag (Canva throws typed `CanvaPendingEnterpriseError`); manual provider works without a flag; library shell at `/(dashboard)/creatives/templates` with per-provider connection state + tooltip explaining what flag to flip. |
| A | — | **deferred** | Depends on `campaign_drafts.event_id` from PR #8. Re-checked PR #8 status after every other task landed — still **OPEN** at end of run. Not started. |
| F | — | **deferred** | Extends Task A's report panel with ticket sales — depends on Task A landing. Not started. |
| G | — | **deferred** | Event ops kanban — also references the post-#8 event linkage. Not started. |

## Open migrations to apply

Apply in numeric order via `supabase db push` (or the Cowork MCP). All
three include RLS + a trailing `notify pgrst, 'reload schema';`.

1. `supabase/migrations/029_ticketing_integration.sql` — `client_ticketing_connections`, `event_ticketing_links`, `ticket_sales_snapshots`.
2. `supabase/migrations/030_d2c_comms.sql` — `d2c_connections`, `d2c_templates`, `d2c_scheduled_sends`.
3. `supabase/migrations/031_creative_templates.sql` — `creative_templates`, `creative_renders`.

After applying, regen types: `npx supabase gen types typescript --project-id zbtldbfjbhfvpksmdvnt > lib/db/database.types.ts`. The DB
helpers in `lib/db/ticketing.ts`, `lib/db/d2c.ts`, and
`lib/db/creative-templates.ts` use a temporary `AnySupabaseClient`
cast that can be dropped once the types regen lands.

## Env vars added (must add to Vercel project settings)

`.env.local.example` is gitignored in this repo, so these only made it
into the per-PR bodies. Set each in Vercel before flipping its
corresponding feature flag.

| Var | Default | Purpose |
|-----|---------|---------|
| `CRON_SECRET` | — | Bearer secret on `/api/cron/sync-ticketing`. **Set this before the first nightly cron fires** or the job will return 401s. |
| `FEATURE_FOURTHEFANS_API` | `false` | Gates the 4TheFans adapter. Flip on once their native API spec lands and `lib/ticketing/fourthefans/client.ts` is filled in. |
| `FOURTHEFANS_API_BASE` | `https://api.4thefans.tv/` | Base URL for 4TheFans calls. Confirm with their team. |
| `FEATURE_D2C_LIVE` | `false` | Gates **all** D2C providers. Defence-in-depth — must be on for any real send to happen. |
| `FEATURE_CANVA_AUTOFILL` | `false` | Pending Canva Enterprise approval. |
| `FEATURE_BANNERBEAR` | `false` | Pending Bannerbear account provisioning. |
| `FEATURE_PLACID` | `false` | Pending Placid account provisioning. |

## Vercel cron

`vercel.json` now schedules `/api/cron/sync-ticketing` daily at
`0 3 * * *` (3 AM UTC). Hobby plan caps cron at one run per day —
upgrade to Pro to bump it back to the originally-spec'd 6-hour
cadence (`0 */6 * * *`).

## Discrepancies noted

- **Spec said `app/clients/[id]/settings/page.tsx`**, repo convention is the dashboard route group. Tasks C and H both placed the settings page at `app/(dashboard)/clients/[id]/settings/page.tsx` so it inherits the dashboard chrome. Same for Task H's per-event comms page (`app/(dashboard)/events/[id]/comms/page.tsx`) and Task I's templates index (`app/(dashboard)/creatives/templates/page.tsx`). Confirm in the morning that this is correct — easy to move if not.
- **`.env.local.example` is gitignored**, so all new env vars are documented in PR bodies + this log only. If you want them tracked, force-add the file in a follow-up commit.
- **Task E**: original spec called for `0 */6 * * *` (every 6 hours). Vercel rejected the deploy on the Hobby plan ("Hobby accounts are limited to daily cron jobs."). Downgraded to `0 3 * * *` to land the PR. Bump back when the team upgrades to Pro.
- **Task A/F/G blocked on PR #8** — see deferral table above.

## What to do in the morning

1. Review PR #8 (Item #1 — event linkage). Once merged, re-run the missing tasks via the same overnight prompt restricted to A, F, G:
   - Task A: `/reports` index + `/reports/[eventId]`. Held back from auto-merge in the original spec — review the KPI strip layout before merging.
   - Task F: extend Task A's panel with ticket-sales pacing.
   - Task G: events kanban at `/events`.
2. Apply migrations 029, 030, 031 (or have the Cowork MCP do it), then regen DB types and drop the `AnySupabaseClient` casts in the three new `lib/db/*` files.
3. Set the env vars listed above in Vercel.
4. Decide on the Vercel plan for the cron cadence.

## Run summary

- **6 of 9 tasks landed** (B, C, D, E, H, I — all auto-merged).
- **3 tasks deferred** pending PR #8 (A, F, G).
- **Lint baseline preserved** on every merged PR (zero new errors/warnings vs `main`).
- **Build green** on `main` after every merge.
- **3 new migrations**, **0 schema-breaking changes**, **0 live external sends**.

