---
description: Always push commits to remote immediately after creating them
alwaysApply: true
---

# Auto-push after every commit

After every `git commit`, immediately run `git push` (pushes to the current tracking branch).

Never leave a commit unpushed. Never use `--force` on `main`.
